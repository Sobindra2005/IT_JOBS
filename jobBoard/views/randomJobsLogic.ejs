<!-- <% 
function generateRandomIndex(arr) {
    return Math.floor(Math.random()*arr.length);
}

function randomJob(jobdata) {
    randomjobs=[];
    const randomIndex=new set();
    while(randomIndex.size<8){
        randomIndex.add(generateRandomIndex(jobdata))
    }
    randomIndex.forEach(index => {
        randomjobs.push(jobdata[index])
    });

    return randomjobs;
}
%> -->